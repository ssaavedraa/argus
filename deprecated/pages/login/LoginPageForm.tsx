'use client'

export default function LoginPageForm() {
  // const cookieStore = cookies()
  // const session = cookieStore.get('session')

  // if (session) {
  //   redirect('/admin/products')
  // }

  // useEffect(() => {
  //   if (state?.isSuccess) {
  //     redirect('/admin/products')
  //   }
  // }, [state])

  return <>{/* <h1 className='text-3xl pb-6 text-center'>Log In</h1> */}</>
}
